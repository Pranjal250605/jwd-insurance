node_modules/
.DS_Store
*.pdf
.next/
dist/
# Host builds are generated per deployment and need CONTACT_EMAIL passed in,
# so they are not committed with a placeholder address baked into them:
#   CONTACT_EMAIL=… npm run build:onamae
build-onamae/
build-milesweb/
# The zips are regenerated by the build; GitHub is not a file host for 33MB
# archives. Run the build command to produce them.
*.zip
# wp-build/ is intentionally NOT ignored: it is the handover build for the
# WordPress host and is committed so it can be downloaded from GitHub.
*.tsbuildinfo
.env
.env.local
.env.*.local
